table:
  name: authors
  schema: public
array_relationships:
  - name: posts
    using:
      foreign_key_constraint_on:
        column: author_id
        table:
          name: posts
          schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - age
        - name
        - id
      filter: {}
